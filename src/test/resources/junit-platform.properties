# 开启 JUnit 扩展自动注册，让 TestTmpCleanupExtension 对全部测试类生效
# （否则 META-INF/services 里的扩展不会被加载）。
# 该扩展只做一件事：整轮测试结束时删除统一测试临时根（%TEMP%/ok-script-toolkit-tests）。
junit.jupiter.extensions.autodetection.enabled=true
